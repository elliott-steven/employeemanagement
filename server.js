var inquirer = require('inquirer');
var connection = require('./connection');

const options = [
    "View Departments",
    "View Roles",
    "View Employees",
    "Update Employee",
    "exit"
];

const empOptions = [
    
]